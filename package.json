{
  "name": "spotify-tui",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "auth": "bun run src/scripts/auth-test.ts",
    "logout": "rm -rf ~/.config/spotify-tui/credentials.json && echo 'Logged out successfully'"
  },
  "dependencies": {
    "@opentui/core": "^0.1.69",
    "open": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "typescript": "^5.0.0"
  }
}
